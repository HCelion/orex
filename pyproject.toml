[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "orex"
description = "Orex"
readme = "README.md"
dependencies = [
    "pytest",
]
version = "1.0.0"

[tool.setuptools.packages.find]
where = ["src"]
